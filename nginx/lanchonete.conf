server {
    listen 80;
    server_name lanchonete;

    # Proxy para o FRONTEND (Vite dev server na VM3 porta 3000)
    location / {
        proxy_pass http://192.168.245.132:3000;
        proxy_http_version 1.1;

        # Necessário para Vite funcionar
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Proxy para o BACKEND (Express na VM3 porta 4000)
    location /api/ {
        proxy_pass http://192.168.245.132:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
